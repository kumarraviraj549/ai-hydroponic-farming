# ===================================================
# HYDROAI BACKEND ENVIRONMENT CONFIGURATION
# ===================================================
# DO NOT commit actual credentials to this repository!
# Copy this file to .env and replace with your values
# ===================================================

# Flask Configuration
FLASK_ENV=development
FLASK_APP=app.py
SECRET_KEY=generate-a-secure-random-key-here-use-secrets-token-hex-32

# Database Configuration
DATABASE_URL=sqlite:///hydroai.db
# For PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:pass@localhost:5432/hydroai

# JWT Configuration
JWT_SECRET_KEY=generate-jwt-secret-with-python-secrets-module
JWT_ACCESS_TOKEN_EXPIRES=3600

# Email Configuration (for alerts)
# SECURITY NOTE: Use App Passwords, not your main Gmail password!
# Generate at: https://myaccount.google.com/apppasswords
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-notifications@yourdomain.com
# CRITICAL: Use App-Specific Password, NOT your main password
MAIL_PASSWORD=your-16-character-app-specific-password

# Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379/0

# External APIs
WEATHER_API_KEY=get-from-openweathermap-api

# Application Settings
DEBUG=true
TESTING=false
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/hydroai.log

# Demo Mode
DEMO_MODE=true

# Security Settings (Production)
# Uncomment and set for production deployment
# SECURE_SSL_REDIRECT=true
# SESSION_COOKIE_SECURE=true
# CSRF_COOKIE_SECURE=true